<phpunit bootstrap="vendor/autoload.php">

    <testsuites>
        <testsuite name="baseline">
            <directory suffix=".php">Tests</directory>
        </testsuite>
    </testsuites>

    <filter>
        <whitelist processUncoveredFilesFromWhitelist="true">
            <directory suffix=".php">src/Ylem/Alpha</directory>
            <directory suffix=".php">src/Ylem/Models</directory>
        </whitelist>
    </filter>

</phpunit>

